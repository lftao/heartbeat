package com.javatao.heartbeat;

import java.util.logging.Logger;

public class HBLoggers {
    public static final Logger logger = Logger.getLogger(HBConfig.class.getName());
    public static final Logger IPortsLogger = Logger.getLogger(IPorts.class.getName());
}
